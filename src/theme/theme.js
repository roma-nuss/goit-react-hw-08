import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    blue: {
      main: "#1976d2",
    },
    secondary: {
      main: "#ff4081",
    },
    red: {
      main: "#ff0000",
    },
  },
});

export default theme;
